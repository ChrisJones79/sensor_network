# Frontend (React + Vite)

## Run

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

## Notes

- Dashboard expects backend API at `VITE_API_BASE_URL`.
- Left panel is fixed to 25% width on desktop and becomes responsive on smaller screens.
